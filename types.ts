export interface ServiceItem {
  title: string;
  description: string;
}

export interface Review {
  author: string;
  text: string;
  rating: number;
}

export interface ProcessStep {
  number: string;
  title: string;
  description: string;
}